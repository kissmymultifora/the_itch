# Configuration file for oneliner.sh
# Lines starting with # are comments

# Field separator for input files (default: |)
FIELD_SEPARATOR=|

# Log level: ERROR, WARN, INFO, DEBUG (default: INFO)
LOG_LEVEL=INFO

# Output format: text, json, csv (default: text)
OUTPUT_FORMAT=text

# Implementation: awk, bash (default: awk)
IMPLEMENTATION=awk
